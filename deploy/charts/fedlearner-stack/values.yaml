nfs-server-provisioner:
  enabled: true

nfs-client-provisioner:
  enabled: false

elastic-stack:
  enabled: true
  elasticsearch:
    master:
      persistence:
        storageClass: nfs
    data:
      persistence:
        storageClass: nfs

  logstash:
    persistence:
      storageClass: nfs
