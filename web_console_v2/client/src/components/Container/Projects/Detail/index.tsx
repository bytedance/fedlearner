import React, { ReactElement } from 'react'
import styled from 'styled-components'

const Container = styled.div`
`

function Detal(): ReactElement {
  return (
    <Container>
      fsdf
    </Container>
  )
}

export default Detal
