import { newlyCreated, withExecutionDetail } from '../example';

const get = () => {
  return {
    data: {
      data: { peer_1: withExecutionDetail || newlyCreated },
    },
    status: 200,
  };
};

export default get;
